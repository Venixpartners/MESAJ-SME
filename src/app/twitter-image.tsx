/**
 * X reads its own tag rather than the Open Graph one, so it gets the same
 * card from the same generator instead of a second design to maintain.
 */
export { default, alt, size, contentType } from "./opengraph-image";
